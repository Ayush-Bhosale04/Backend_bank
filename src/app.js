// use of this folder is to create the server instincs and to config the server (kon kon se middle ware hum use kar rahe hai,kitne type ke api hoge etc)


const express = require('express');

const app = express();

module.exports = app;